import SectionHtml from "@/components/shared/SectionHtml";

/** Bootstrap YouTube modal markup (legacy `data-dismiss` + `site.js` / `previousevent.js`). */
export default function VideoModal() {
  return <SectionHtml file="video-modal.html" />;
}
