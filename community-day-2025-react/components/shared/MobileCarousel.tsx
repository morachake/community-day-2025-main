/**
 * Mobile carousel behavior for speakers / organizers / volunteers is implemented in
 * `/public/js/home-after.js` (ported from the legacy `index.html` inline scripts)
 * and runs after hydration.
 */
export default function MobileCarousel() {
  return null;
}
